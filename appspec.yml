version: 0.0
os: linux
files:
  - source: /app
    destination: /opt/myapp
  - source: /appspec.yml
    destination: /opt/myapp
hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 800
      runas: root
